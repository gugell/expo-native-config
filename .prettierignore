pnpm-lock.yaml
node_modules
dist
artifacts
apps/*/ios
apps/*/android
# Generated by release-it from the commit history. Prettier reflows its
# hard-wrapped body text, so formatting it would make `pnpm check` fail on the
# release commit itself — the check CI runs on main right after a release.
**/CHANGELOG.md
